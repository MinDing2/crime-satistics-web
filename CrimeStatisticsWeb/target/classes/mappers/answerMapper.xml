<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Answer">
	<!-- 댓글 조회 -->
<select id="AnswerList" parameterType="int" resultType="com.spring.answer.vo.AnswerVo">
	 SELECT
     		answer_id, question_id, answer_cont,create_date
        FROM
            answer 
        WHERE
            question = #{question_id}	
</select>

<!-- 댓글 작성 -->
<insert id="AnswerWrite" parameterType="com.spring.answer.vo.AnswerVo">
	insert into answer(question_id, answer_cont, create_date)
		value(#{question_id}, #{answer_cont}, #{create_date})
</insert>
	
<!-- 댓글 수정 -->
<update id="AnswerModify" parameterType="com.spring.answer.vo.AnswerVo">
	UPDATE answer SET
     
            answer_cont = #{answer_cont},
   
        WHERE
            answer_id = #{answer_id}
</update>

<!-- 댓글 삭제 -->
<delete id="AnswerDelete" parameterType="com.spring.answer.vo.AnswerVo">
	delete from answer
	where  answer_id = #{answer_id}	
		and question_id = ${question_id}
</delete>
</mapper>